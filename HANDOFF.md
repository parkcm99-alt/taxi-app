# 🔄 작업 인계서 (HANDOFF)

> **이 파일을 먼저 읽으세요.** 이전 세션의 대화 맥락 없이도 이어서 작업할 수 있도록 정리한 문서입니다.
> **작업을 진행할 때마다 이 파일의 "진행 상태"와 "다음 할 일"을 갱신하세요.**

**최종 갱신:** 2026-08-03 11:40 (Codex · GitHub 동기화)
**프로젝트 루트:** `/Users/parkyoungsun/Desktop/taxi-app`

---

## 1. 재개 시 가장 먼저 할 것 (3분)

```bash
cd /Users/parkyoungsun/Desktop/taxi-app && python3 tools/parse.py /tmp/check.json
```

정상이라면 이렇게 나옵니다:
```
택시_기출_텍스트_Q001-Q070_승인본.md                     69문항  오류 0
택시_기출_텍스트_Q071-Q160.md                         89문항  오류 0
택시_기출_텍스트_Q161-Q240.md                         74문항  오류 0
택시_CBT_제1회~제7회 각 70문항  오류 0
총 722문항
과목별: {1: 238, 2: 211, 3: 203, 4: 70}
```

- **`총 722문항`이어야 정상**입니다. 다르면 새 문서나 과목 경계를 먼저 확인하세요.
- **`오류`가 0이 아니다** → 그 마크다운 형식이 깨진 것. 먼저 고칠 것.
- 그다음 이 파일의 **2번 진행 상태**를 읽으세요.

---

## 2. 진행 상태

### ✅ 완료 (건드릴 필요 없음)

| 항목 | 결과 |
|---|---|
| CBT 1·2회 | 각 70문항 (이전 세션에서 완료) |
| **CBT 3·4·5·6회** | 각 70문항. **사용자 확정 정답표와 100% 일치 검증 완료** |
| **CBT 7회** | 70문항. 사전 정답표가 없어 **사진 신규 판독**(회전 방향 2가지로 교차 확인) |
| 텍스트 기출 Q1~Q70 | 69문항 (Q23은 Q1과 중복이라 결번). 사용자 승인 완료 |
| **텍스트 기출 Q71~Q160** | 89문항. Q150 중복 결번, 복원 13문항, 파서 오류 0 |
| **텍스트 기출 Q161~Q240** | 74문항. 중복 6문항 결번, 복원 6문항, 파서 오류 0 |
| **과목 경계** | `TEXT_SVC_START = 173` 반영 완료 |
| **앱 빌드** | `index.html` 약 364KB 단일 파일, 총 722문항. 아래 검증 전부 통과 |
| **GitHub 소스 동기화** | `parkcm99-alt/taxi-app` 공개 저장소 `main` 브랜치 연결·최초 업로드 완료 |
| README·지침서 | `README.md`, `CODEX_지침서.md` |

**앱 검증 결과 (2026-08-03 완료):**
- 과목 배분 20/20/20/10 정확
- 셔플 후 정답 매핑 70문항 전부 오류 0
- 합격 경계 41→불합격 / 42→합격
- 채점·오답노트·기록 저장 정상
- 그림 SVG 15개 매핑 무결, 파싱 오류 0, 육안 확인 완료

### 🔲 미완료

| 항목 | 담당 | 상태 |
|---|---|---|
| ⚠️복원 19문항 사용자 승인 | 사용자 | 새 문서의 특이사항 표 확인 필요 |
| GitHub Pages 배포 | — | **사용자 확인 대기** (아래 4번) |

---

## 3. 다음 할 일 (순서대로)

### A. 사용자 검토

새 문서의 `⚠️ 특이사항`에서 복원 19문항의 질문·정답 내용을 확인받습니다. Q71~Q240에는 신규 그림 문항이 없습니다.

### B. 수정 후 재빌드
```bash
python3 tools/parse.py /tmp/questions.json
```
```bash
python3 - <<'EOF'
tpl = open("tools/app_template.html", encoding="utf-8").read()
data = open("/tmp/questions.json", encoding="utf-8").read()
open("index.html", "w", encoding="utf-8").write(tpl.replace("__QUESTIONS__", data))
print("index.html 생성 완료")
EOF
```

### C. 배포
아래 4번 참조.

---

## 4. 사용자 확인이 필요한 미결정 사항

| # | 내용 | 상태 |
|:-:|---|---|
| 1 | **GitHub Pages 배포** — 소스는 `parkcm99-alt/taxi-app`에 동기화됐으며 Pages 활성화만 남음 | ❓ 대기 |
| 2 | 새 문서의 **⚠️복원 19문항** 승인 | ❓ 사용자 검토 대기 |
| 3 | 배포 시점 — 현재 722문항 빌드로 배포할지 | ❓ 대기 |

**소스 동기화는 승인·완료됐습니다. GitHub Pages 활성화는 사용자 요청 후 진행하세요.**

---

## 5. ⚠️ 함정 모음 (이 세션에서 실제로 겪은 것)

### 5-1. 교재 사진은 90° 회전되어 있다
`images/*/` 원본은 세로로 촬영되어 그대로는 판독이 어렵습니다. **시계방향 90° 회전** 후 읽으세요.
```bash
cp images/3회/3-01.jpeg /tmp/x.jpeg && sips -r 90 /tmp/x.jpeg -Z 2000
```
- **예외: `images/7회/7-15.jpeg`는 회전 방향이 반대**입니다 (`sips -r -90`).
- 판독이 애매하면 **양방향으로 회전해 교차 확인**하세요. 7회 Q66~70을 그렇게 확정했습니다.

### 5-2. 정답 분포 표를 손으로 세지 말 것
CBT 3·4·5·6·7회에서 **손계산으로 5번 중 5번 다 틀렸습니다.** 반드시 스크립트로:
```bash
python3 tools/verify.py "docs/택시_CBT_제3회_70문항.md" "01② 02③ 03③ ... 70①"
```
`verify.py`가 문항 수·정답 수·누락·본문↔정답표 대조·분포를 한 번에 출력합니다.

### 5-3. 선택지 구분자는 ` · ` (앞뒤 공백)
파서가 이걸로 4조각을 냅니다. **선택지 내용 안에 공백 낀 가운뎃점이 들어가면 파싱이 깨집니다.**
- ❌ `① 공 · 사를 구분` → 5조각
- ✅ `① 공·사를 구분`

### 5-4. Preview 서버는 Desktop 폴더에 접근할 수 없다 (macOS TCC)
`python3 -m http.server`를 `.claude/launch.json`으로 띄우면 `PermissionError: Operation not permitted`가 납니다. Bash 도구는 되는데 preview 프로세스만 막힙니다.

**우회법:** 스크래치패드에 `index.html`을 복사하고 거기서 서빙.
```bash
S=<스크래치패드경로>/serve
mkdir -p $S && cp index.html $S/
# $S/server.py 에 os.chdir("$S") 후 http.server 실행
```
현재 `.claude/launch.json`은 이 방식으로 설정되어 있으며 **스크래치패드 경로는 세션마다 바뀝니다.** 새 세션에서는 경로를 다시 잡아야 합니다.

사용자가 직접 볼 때는 이걸로 충분합니다:
```bash
python3 tools/serve.py
```

### 5-5. `index.html`을 직접 수정하지 말 것
마크다운에서 자동 생성되는 산출물입니다. 직접 고치면 다음 빌드에서 **전부 사라집니다.**
문제 내용은 `docs/*.md`를, 앱 로직·CSS·SVG는 `tools/app_template.html`을 고치세요.

### 5-6. `answer`는 0-based
`①=0, ②=1, ③=2, ④=3`. 마크다운의 `(정답)` 위치에서 파서가 자동 계산하므로 사람이 손댈 일은 없지만, 앱 로직을 건드릴 때 가장 실수가 잦은 부분입니다.

### 5-7. 터널 그림 3개는 서로 다르다
2회 Q36 / 3회 Q37 / 5회 Q32는 모두 "터널 안전수칙" 계열이지만 **그림이 달라 정답도 다릅니다.** SVG를 재사용하면 안 됩니다.
반대로 **1회 Q13과 7회 Q10은 완전히 같은 그림**(백색 지그재그=서행)이라 SVG를 공유합니다.

### 5-8. STT 원문에서 숫자 2가 '이'로 인식된다
`source/*.txt`는 음성인식 결과입니다. `"정답은이 XXX"` = `"정답은 2. XXX"`.
전체 오타 사전은 `CODEX_지침서.md` 5번에 정리해 두었습니다.

---

## 6. 파일 소유권 (병렬 작업 시 충돌 방지)

| 파일 | 소유 | 비고 |
|---|---|---|
| `docs/택시_기출_텍스트_Q071-Q160.md` | **Codex** | 신규 생성 |
| `docs/택시_기출_텍스트_Q161-Q240.md` | **Codex** | 신규 생성 |
| `index.html` | **Claude** | Codex 접근 금지 (재생성됨) |
| `tools/*` | **Claude** | |
| 기존 `docs/*.md` 8개 | 잠금 | 검증 완료. 수정 금지 |
| `source/*`, `images/*` | 읽기 전용 | 원본 |

---

## 7. 핵심 규격 요약 (빠른 참조)

```
시험      70문항 / 70분 / 42문항(60%) 합격
과목      ①법규20 ②안전운행20 ③운송서비스20 ④서울지리10
보유      559문항 (법규209 · 안전운행140 · 운송서비스140 · 지리70)
저장소키  taxiExamStats_v1   ← 버스 앱 busExamStats_v3와 반드시 분리
파서출력  { id, subject, q, choices[4], answer(0-based), exp, img }
ID접두어  t- (텍스트, 23번 결번) / c1-~c7- (CBT 1~7회)
그림      15개 (FIG + FIGMAP, tools/app_template.html 내부)
```

**마크다운이 유일한 진실입니다.** `docs/*.md` → `parse.py` → `index.html`.

---

## 8. 인계서 갱신 규칙

작업을 마칠 때마다 이 파일에서 다음을 고치세요.

1. 맨 위 **최종 갱신** 날짜·주체
2. **2번 진행 상태** 표 (✅ / 🔲 이동)
3. **3번 다음 할 일** 에서 끝난 단계 삭제
4. 새로 겪은 함정이 있으면 **5번**에 추가 ← 가장 가치 있는 부분
5. 사용자 결정이 나오면 **4번** 반영

이 파일이 최신이면 어떤 세션에서든 3분 안에 재개할 수 있습니다.
